const sum = (fNum, sNum) => {
    return fNum + sNum
};

const sub = (fNum, sNum) => {
    return fNum - sNum
};


const multiplication = (fNum, sNum) => {
    return fNum  * sNum
};




module.exports = sum;
module.exports = sub;
module.exports = multiplication;
